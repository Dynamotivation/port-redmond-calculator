#pragma once

// Preserve Microsoft's <winerror.h> include without changing its source. The
// repository already carries the HRESULT-compatible definitions used by the
// portable calculator engine.
#include "CalcManager/winerror_cross_platform.h"
