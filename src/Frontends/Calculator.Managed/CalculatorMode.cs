namespace Calculator.Managed;

// Values intentionally match calculator_mode in CalculatorNative.h.
public enum CalculatorMode
{
    Standard = 0,
    Scientific = 1,
    Programmer = 2,
}

public enum CalculatorAngleMode
{
    Degrees,
    Radians,
    Grads,
}
